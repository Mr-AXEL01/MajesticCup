spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/${DB_NAME}
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
      database: ${DB_NAME}

logging:
  level:
    org:
      springframework:
        data:
          mongodb: DEBUG
